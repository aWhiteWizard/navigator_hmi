<Window x:Class="NavigatorHMI.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用户" Width="320" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" ResizeMode="NoResize" FontSize="12">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="用户名:" VerticalAlignment="Center"/>
            <TextBox x:Name="NameBox" Grid.Column="1" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
        </Grid>
        <Grid Grid.Row="1" Margin="0,6,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="密码:" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PassBox" Grid.Column="1" PasswordChanged="PassBox_PasswordChanged" Padding="2"/>
        </Grid>
        <TextBlock Grid.Row="2" Text="（编辑时密码留空=不改）" FontSize="10" Foreground="Gray" Margin="70,2,0,0" IsHitTestVisible="False"/>
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="所属组:" VerticalAlignment="Center"/>
            <ComboBox x:Name="GroupCombo" Grid.Column="1" Padding="2"/>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" Width="70" Height="28" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="确定" Width="70" Height="28" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
