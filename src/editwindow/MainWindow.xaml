<Window x:Class="editwindow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:editwindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="文件(N)">
                <MenuItem Header="新建(N)"/>
                <MenuItem Header="打开(O)"/>
                <MenuItem Header="保存(S)"/>
                <MenuItem Header="另存为(A)"/>
                <MenuItem Header="关闭当前项目(X)"/>
                <MenuItem Header="退出(Q)"/>
            </MenuItem>
            <MenuItem Header="编辑(E)"/>
            <MenuItem Header="查看(V)"/>
        </Menu>
    </Grid>
</Window>
